// Writing routes are not generated until Drew explicitly approves publication.
// This is a publishing switch, not access control for the public Git repository.
export const features = { writing: false } as const;
